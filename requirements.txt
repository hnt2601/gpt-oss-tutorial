# Core dependencies
# Note: OpenAI SDK version requirements for GPT-OSS API
# - Needs >= 1.50.0 for responses API support
# - Must be < 2.0.0 to avoid breaking validation changes
openai>=1.50.0,<2.0.0
packaging>=23.0
python-dotenv>=1.0.0

# Data processing
requests>=2.31.0
pydantic>=2.0.0

# Image processing (for multimodal examples)
Pillow>=10.0.0

# For RAG examples
datasets>=2.14.0
pandas>=2.0.0
tqdm>=4.65.0

# Vector database (optional, for RAG)
pinecone-client>=3.0.0

# For Jupyter notebook examples (optional)
ipython>=8.12.0
jupyter>=1.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Code quality (optional)
ruff>=0.1.0
black>=23.0.0

